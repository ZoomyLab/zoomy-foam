FoamFile
{
    format      ascii;
    class       volScalarField;
    object      Q0;
}

dimensions      [0 0 0 0 0 0 0];   // b — dimensionless tracker (matches solver)
internalField   uniform 0;

boundaryField
{
    inflow        { type zeroGradient; }
    outflow       { type zeroGradient; }
    sides         { type empty; }
    topAndBottom  { type empty; }
}
