FoamFile
{
    format      ascii;
    class       volScalarField;
    object      Q1;
}

dimensions      [0 0 0 0 0 0 0];   // h — dimensionless (init.H sets dimless)
internalField   uniform 0.01;       // overridden by setFields → h_L on left

boundaryField
{
    inflow        { type zeroGradient; }
    outflow       { type zeroGradient; }
    sides         { type empty; }
    topAndBottom  { type empty; }
}
